name: RT02-postinstall-curl
on: [pull_request]
jobs:
  rt02:
    runs-on: ubuntu-latest
    steps:
      - name: RT02 — loopback curl (127.0.0.1 always in egress allowlist)
        run: curl -s --connect-timeout 1 http://127.0.0.1:9999 || true
